<!-- replace "span.sr-only" -->

<% if provider == :saml %>
  <span><%= t("devise.login.sign_in_saml") %></span>
<% else %>
  <span class="sr-only"><%= t("devise.shared.links.sign_in_with_provider", provider: normalize_provider_name(provider).titleize) %></span>
<% end %>
